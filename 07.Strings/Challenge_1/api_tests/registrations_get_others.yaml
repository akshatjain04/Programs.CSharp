# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /recipients/persons/{id}/registrations_get for http method type GET in artillery framework
# RoostTestHash=318c363c90



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "id"
      - "credential-url"
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Response Testing"
    flow:
      - comment: "Sending request with invalid UUID in path parameter"
      - get:
          url: "/recipients/persons/invalid-uuid/registrations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
      - think: 1
      - expect:
          - statusCode: 400
      - expect:
          - hasHeader: "Content-Type"
      - expect:
          - headerEquals: { "Content-Type": "application/json" }

      - comment: "Sending request without authentication token in headers"
      - get:
          url: "/recipients/persons/{{ id }}/registrations"
          headers: {}
      - think: 1
      - expect:
          - statusCode: 401
      - expect:
          - hasHeader: "Content-Type"
      - expect:
          - headerEquals: { "Content-Type": "application/json" }

      - comment: "Sending request with invalid authentication token"
      - get:
          url: "/recipients/persons/{{ id }}/registrations"
          headers:
            Token: "invalid_token"
      - think: 1
      - expect:
          - statusCode: 403
      - expect:
          - hasHeader: "Content-Type"
      - expect:
          - headerEquals: { "Content-Type": "application/json" }

      - comment: "Sending request with size exceeding maximum limit"
      - get:
          url: "/recipients/persons/{{ id }}/registrations?size=51"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
      - think: 1
      - expect:
          - statusCode: 422
      - expect:
          - hasHeader: "Content-Type"
      - expect:
          - headerEquals: { "Content-Type": "application/json" }

      - comment: "Sending request with invalid offset"
      - get:
          url: "/recipients/persons/{{ id }}/registrations?offset=-1"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
      - think: 1
      - expect:
          - statusCode: 422
      - expect:
          - hasHeader: "Content-Type"
      - expect:
          - headerEquals: { "Content-Type": "application/json" }
