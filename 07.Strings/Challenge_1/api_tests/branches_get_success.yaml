# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /recipients/branches_get for http method type GET in artillery framework
# RoostTestHash=74a40da00e



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "branches_get_success.csv"
    fields:
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}
  defaults:
    headers:
      Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Validate successful responses for /recipients/branches"
    flow:
      - get:
          url: "/recipients/branches?coc-number={{ coc-number }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "total"
            - hasProperty: "branches"
      - think: 1

      - get:
          url: "/recipients/branches?coc-branch-number={{ coc-branch-number }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "total"
            - hasProperty: "branches"
      - think: 1

      - get:
          url: "/recipients/branches?trade-name={{ trade-name }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "total"
            - hasProperty: "branches"
      - think: 1

      - get:
          url: "/recipients/branches?size={{ size }}&offset={{ offset }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "total"
            - hasProperty: "branches"
      - think: 1
