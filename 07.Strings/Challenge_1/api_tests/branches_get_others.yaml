# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /recipients/branches_get for http method type GET in artillery framework
# RoostTestHash=74a40da00e



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "branches_get_others.csv"
    fields:
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - flow:
      # Invalid API Key
      - post:
          url: "/recipients/branches"
          headers:
            Token: "InvalidApiKey"
          expect:
            - statusCode: 401
      - think: 1

      # Missing API Key
      - post:
          url: "/recipients/branches"
          headers:
            Token: ""
          expect:
            - statusCode: 401
      - think: 1

      # Invalid coc-number (not matching pattern)
      - get:
          url: "/recipients/branches?coc-number=123"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      # Invalid coc-branch-number (not matching pattern)
      - get:
          url: "/recipients/branches?coc-branch-number=123"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      # Invalid size (below minimum)
      - get:
          url: "/recipients/branches?size=0"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      # Invalid size (above maximum)
      - get:
          url: "/recipients/branches?size=51"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      # Invalid offset (below minimum)
      - get:
          url: "/recipients/branches?offset=-1"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      # Invalid URL case
      - get:
          url: "/invalidpath/branches"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
