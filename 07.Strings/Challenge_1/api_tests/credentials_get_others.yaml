# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=66f52a572f



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "invalidSize"
      - "invalidOffset"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-successful API Request Scenario"
    flow:
      - get:
          url: "/credentials?size={{ invalidSize }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 2
      - get:
          url: "/credentials?offset={{ invalidOffset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 2
      - get:
          url: "/credentials"
          headers:
            Token: "InvalidTokenValue"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 2
      - get:
          url: "/credentials"
          headers: {}
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 2
      - get:
          url: "/invalid_credentials"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 2
