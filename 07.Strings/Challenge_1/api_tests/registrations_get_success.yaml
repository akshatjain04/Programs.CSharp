# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /recipients/persons/{id}/registrations_get for http method type GET in artillery framework
# RoostTestHash=318c363c90



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "registrations_get_success.csv"
    fields:
      - "id"
      - "credential-url"
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful API responses for /recipients/persons/{id}/registrations"
    flow:
      - get:
          url: "/recipients/persons/{{ id }}/registrations?credential-url={{ credential-url }}&size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals:
                api-version: "1.0.0"
            - hasProperty: "total"
            - hasProperty: "registrations"
      - think: 1

      - get:
          url: "/recipients/persons/{{ id }}/registrations?size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
            - hasProperty: "total"
            - hasProperty: "registrations"
      - think: 1

      - get:
          url: "/recipients/persons/{{ id }}/registrations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "total"
            - hasProperty: "registrations"
      - think: 1
