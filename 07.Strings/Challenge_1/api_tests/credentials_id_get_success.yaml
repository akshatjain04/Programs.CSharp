# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=018c225aea



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful API responses for /credentials/{id}"
    flow:
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "id"
            - hasProperty: "format"
            - hasProperty: "credential"
            - hasProperty: "status"
            - hasProperty: "statusUpdatedAt"
            - hasProperty: "createdAt"
      - think: 1
