# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=018c225aea



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses Test Scenario"
    flow:
      - comment: "Expecting a 400 Bad Request due to invalid ID format in URL."
      - get:
          url: "/credentials/invalid-id-format"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
            - headerEquals:
                content-type: "application/json"
      - think: 1

      - comment: "Expecting a 401 Unauthorized due to missing authentication token."
      - get:
          url: "/credentials/{{ id }}"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
            - headerEquals:
                content-type: "application/json"
      - think: 1

      - comment: "Expecting a 403 Forbidden due to invalid authentication token."
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "invalidAuthToken"
          expect:
            - statusCode: 403
            - hasHeader: "content-type"
            - headerEquals:
                content-type: "application/json"
      - think: 1

      - comment: "Expecting a 404 Not Found for a non-existent ID."
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
            - headerEquals:
                content-type: "application/json"
      - think: 1

      - comment: "Expecting a 405 Method Not Allowed when using POST on a GET endpoint."
      - post:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalid: "data"
          expect:
            - statusCode: 405
            - hasHeader: "content-type"
            - headerEquals:
                content-type: "application/json"
      - think: 1
