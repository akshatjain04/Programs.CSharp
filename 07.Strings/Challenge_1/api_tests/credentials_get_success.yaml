# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=66f52a572f



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "credentials_get_success.csv"
    fields:
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful API responses for /credentials endpoint"
    flow:
      - get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
            - hasProperty: "credentials"
            - hasProperty: "total"
            - jmespath: "credentials[*].id | length(@) > 0"
            - jmespath: "credentials[*].status | length(@) > 0"
      - think: 1
