# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /recipients/branches/{id}_get for http method type GET in artillery framework
# RoostTestHash=061355b093



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "branches_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful responses for various API requests"
    flow:
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
      - think: 2
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasHeader: "api-version"
      - think: 2
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "id"
      - think: 2
      # ... Repeat this pattern for each expectation, splitting into separate gets
      # Add a 'think' action after each 'get' if required by the original scenario flow
