# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /recipients/persons_get for http method type GET in artillery framework
# RoostTestHash=aa0a5d4980



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "persons_get_success.csv"
    skipHeader: true
    fields:
      - "size"
      - "offset"
      - "name"
      - "birth-date"
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful API responses for /recipients/persons endpoint"
    flow:
      - get:
          url: "/recipients/persons?size={{ size }}&offset={{ offset }}&name={{ name }}&birth-date={{ birth-date }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
            - hasProperty: "total"
            - hasProperty: "persons"
            - jmespath: "persons[*].id"
            - jmespath: "persons[*].initials"
            - jmespath: "persons[*].firstName"
            - jmespath: "persons[*].lastName"
            - jmespath: "persons[*].birthDate"
            - jmespath: "persons[*].createdAt"
            - jmespath: "persons[*].updatedAt"
      - think: 1
