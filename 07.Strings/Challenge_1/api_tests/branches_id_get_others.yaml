# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /recipients/branches/{id}_get for http method type GET in artillery framework
# RoostTestHash=061355b093



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "branches_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-success scenarios for /recipients/branches/{id}"
    flow:
      # Invalid UUID in the path parameter
      - get:
          url: "/recipients/branches/invalid-uuid"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
            - headerEquals: 
                content-type: "application/json"
      - think: 2

      # Missing authentication token
      - get:
          url: "/recipients/branches/{{ id }}"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
            - headerEquals: 
                content-type: "application/json"
      - think: 2

      # Incorrect authentication token
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            Token: "incorrect-token"
          expect:
            - statusCode: 403
            - hasHeader: "content-type"
            - headerEquals: 
                content-type: "application/json"
      - think: 2

      # Valid UUID but not found
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
            - headerEquals: 
                content-type: "application/json"
      - think: 2

      # Incorrect URL
      - get:
          url: "/recipients/incorrect-url/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
            - headerEquals: 
                content-type: "application/json"
      - think: 2
