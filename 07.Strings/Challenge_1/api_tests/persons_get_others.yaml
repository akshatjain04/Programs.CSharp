# ********RoostGPT********
# Test generated by RoostGPT for test MiniProjects using AI Type Open AI and AI Model gpt-4-1106-preview

# Test generated for /recipients/persons_get for http method type GET in artillery framework
# RoostTestHash=aa0a5d4980



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "persons_get_others.csv"
    fields:
      - "size"
      - "offset"
      - "name"
      - "birth-date"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "API Error Handling Scenario"
    flow:
      - get:
          url: "/recipients/persons?size={{ size }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
            - headerEquals: { "content-type": "application/json; charset=utf-8" }
      - think: 1

      - get:
          url: "/recipients/persons?offset={{ offset }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
            - headerEquals: { "content-type": "application/json; charset=utf-8" }
      - think: 1

      - get:
          url: "/recipients/persons?name={{ name }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
            - headerEquals: { "content-type": "application/json; charset=utf-8" }
      - think: 1

      - get:
          url: "/recipients/persons?birth-date={{ birth-date }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
            - headerEquals: { "content-type": "application/json; charset=utf-8" }
      - think: 1

      - get:
          url: "/recipients/persons"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
            - headerEquals: { "content-type": "application/json; charset=utf-8" }
      - think: 1

      - get:
          url: "/recipients/persons"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 403
            - hasHeader: "content-type"
            - headerEquals: { "content-type": "application/json; charset=utf-8" }
      - think: 1

      - get:
          url: "/invalid_url"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
            - headerEquals: { "content-type": "application/json; charset=utf-8" }
      - think: 1
